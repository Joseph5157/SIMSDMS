# Suppress npm deprecation warnings and use modern npm config
legacy-peer-deps=false
audit=true
fund=false
save-exact=false

# Use --omit=dev instead of deprecated --production flag
# This is handled by NODE_ENV=production in Railway
